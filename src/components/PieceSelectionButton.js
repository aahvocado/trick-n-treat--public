import React, { PureComponent } from 'react';

/**
 * page to select the pieces
 */
class PieceSelectionButton extends PureComponent {
  render() {
    return (
      <button className='flex-centered pad-2 bor-1'>
        select your piece
      </button>
    )
  }
}

export default PieceSelectionButton.js;
